# DATAVIZ project

## The App

### JSON Rest API

The app provides raw json data via the routes under /api/raw

- /api/raw/data
- /api/raw/
- /api/raw/

### The HTML views

- / (Heatmap View grouped by user and session)
  
